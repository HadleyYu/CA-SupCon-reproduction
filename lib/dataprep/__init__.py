"""Compatibility module for the original CA-SupCon entrypoint.

The public upstream repository imports ``dataprep`` but does not include
data-preparation code. Reproduction scripts are kept under ``scripts/``.
"""
